# Bahndata

Bahndata is a simple node.js server that queries the public deutschebahn API for the timetable of the stations tier 3 and above. The data is stored in a PostgreSQL database which can be set up with the commands in the postgre folder
